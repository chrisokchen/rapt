# Generated by rapt-form-haapi
# DSL: haAPI v3.3.0
api: store
schema_version: "3.3"
entity: Store
title: 店面 API
description: 店面基本資料維護與平台檢視

exposes:
  standard: [list, read, update]
  list:
    filters:
      - {field: merchantId, operators: [eq]}
      - {field: status, operators: [eq]}
    sorting: {fields: [createdAt], default: createdAt:desc}
    pagination: {style: offset, default_size: 20}

access:
  authentication: {type: bearer, required: true}
  endpoints:
    list:
      required_roles: [platform-admin]
      required_permissions:
        - id: store_list_all
    read:
      required_roles: [merchant, platform-admin]
      required_permissions:
        - id: store_read_own
        - id: store_read_all
    update:
      required_roles: [merchant]
      required_permissions:
        - id: store_update_own

source_evidence:
  - "docs/ssot/habdd/merchant-onboarding.ha.feature#Scenario:平台開通商家店面"
  - "docs/ssot/dbml/schema.dbml#Table:Store"
  - "docs/ssot/haarm/smallBiz.haarm.yaml#roles"
