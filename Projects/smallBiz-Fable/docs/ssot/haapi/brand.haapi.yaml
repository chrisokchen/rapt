# Generated by rapt-form-haapi
# DSL: haAPI v3.3.0
api: brand
schema_version: "3.3"
entity: Brand
title: 品牌 API
description: 商品品牌維護

exposes:
  standard: [list, read, create, update]
  list:
    filters:
      - {field: storeId, operators: [eq]}
    sorting: {fields: [name], default: name:asc}
    pagination: {style: offset, default_size: 50}

access:
  authentication: {type: bearer, required: true}
  endpoints:
    list:
      required_roles: [public, merchant]
    read:
      required_roles: [public, merchant]
    create:
      required_roles: [merchant]
      required_permissions:
        - id: brand_create_store
    update:
      required_roles: [merchant]
      required_permissions:
        - id: brand_update_store

source_evidence:
  - "docs/ssot/habdd/product-management.ha.feature#Scenario:建立商品並儲存為草稿"
  - "docs/ssot/dbml/schema.dbml#Table:Brand"
  - "docs/ssot/haarm/smallBiz.haarm.yaml#roles"
