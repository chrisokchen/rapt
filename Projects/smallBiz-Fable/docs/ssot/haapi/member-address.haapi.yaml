# Generated by rapt-form-haapi
# DSL: haAPI v3.3.0
api: member-address
schema_version: "3.3"
entity: MemberAddress
title: 收件地址 API
description: 會員常用收件地址維護

exposes:
  standard: [list, create, update, delete]
  list:
    filters:
      - {field: memberId, operators: [eq]}
    pagination: {style: offset, default_size: 20}

access:
  authentication: {type: bearer, required: true}
  endpoints:
    list:
      required_roles: [consumer]
      required_permissions:
        - id: member_address_list_own
    create:
      required_roles: [consumer]
      required_permissions:
        - id: member_address_create_own
    update:
      required_roles: [consumer]
      required_permissions:
        - id: member_address_update_own
    delete:
      required_roles: [consumer]
      required_permissions:
        - id: member_address_delete_own

source_evidence:
  - "docs/ssot/habdd/member-account.ha.feature#Scenario:儲存常用收件地址"
  - "docs/ssot/dbml/schema.dbml#Table:MemberAddress"
  - "docs/ssot/haarm/smallBiz.haarm.yaml#roles"
