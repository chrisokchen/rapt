# Generated by rapt-form-haapi
# DSL: haAPI v3.3.0
api: platform-user
schema_version: "3.3"
entity: PlatformUser
title: 平台人員 API
description: 平台管理員與行銷人員帳號維護

exposes:
  standard: [list, read, create, update]
  list:
    filters:
      - {field: roleCode, operators: [eq]}
      - {field: isActive, operators: [eq]}
    pagination: {style: offset, default_size: 20}

access:
  authentication: {type: bearer, required: true}
  endpoints:
    list:
      required_roles: [platform-admin]
    read:
      required_roles: [platform-admin]
    create:
      required_roles: [platform-admin]
      required_permissions:
        - id: platform_user_manage
    update:
      required_roles: [platform-admin]
      required_permissions:
        - id: platform_user_manage

source_evidence:
  - "docs/discovery/01-stakeholders.md#platform-admin"
  - "docs/ssot/dbml/schema.dbml#Table:PlatformUser"
  - "docs/ssot/haarm/smallBiz.haarm.yaml#roles"
