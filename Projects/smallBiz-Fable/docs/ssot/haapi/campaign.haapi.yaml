# Generated by rapt-form-haapi
# DSL: haAPI v3.3.0
api: campaign
schema_version: "3.3"
entity: Campaign
title: 促銷活動 API
description: 有檔期的促銷活動建立與自動上下架

exposes:
  standard: [list, read, create, update]
  list:
    filters:
      - {field: storeId, operators: [eq]}
      - {field: campaignType, operators: [eq]}
      - {field: status, operators: [eq]}
    sorting: {fields: [startAt], default: startAt:desc}
    pagination: {style: offset, default_size: 20}

access:
  authentication: {type: bearer, required: true}
  endpoints:
    list:
      required_roles: [merchant, platform-marketer]
    read:
      required_roles: [merchant, platform-marketer]
    create:
      required_roles: [merchant]
      required_permissions:
        - id: campaign_create_store
    update:
      required_roles: [merchant, platform-marketer]
      required_permissions:
        - id: campaign_update_store
        - id: campaign_manage_all

source_evidence:
  - "docs/ssot/habdd/promotion-campaign.ha.feature#Scenario:建立有檔期的促銷活動"
  - "docs/ssot/habdd/promotion-campaign.ha.feature#Scenario:檔期開始時活動自動生效"
  - "docs/ssot/dbml/schema.dbml#Table:Campaign"
  - "docs/ssot/haarm/smallBiz.haarm.yaml#roles"
