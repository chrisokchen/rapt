# Generated by rapt-form-haapi
# DSL: haAPI v3.3.0
api: merchant
schema_version: "3.3"
entity: Merchant
title: 商家帳號 API
description: 商家註冊、平台開通與停權

exposes:
  standard: [list, read, create, update]
  list:
    filters:
      - {field: status, operators: [eq]}
    sorting: {fields: [createdAt], default: createdAt:desc}
    pagination: {style: offset, default_size: 20}
  operations:
    - name: activate
      method: POST
      path: /{id}/activate
    - name: suspend
      method: POST
      path: /{id}/suspend

access:
  authentication: {type: bearer, required: true}
  endpoints:
    create:
      required_roles: [public]
    list:
      required_roles: [platform-admin]
      required_permissions:
        - id: merchant_list
    read:
      required_roles: [platform-admin]
      required_permissions:
        - id: merchant_read
    update:
      required_roles: [platform-admin]
      required_permissions:
        - id: merchant_update_status
  operations:
    activate:
      required_roles: [platform-admin]
      required_permissions:
        - id: merchant_update_status
    suspend:
      required_roles: [platform-admin]
      required_permissions:
        - id: merchant_update_status

source_evidence:
  - "docs/ssot/habdd/merchant-onboarding.ha.feature#Scenario:商家註冊開店申請"
  - "docs/ssot/habdd/merchant-onboarding.ha.feature#Scenario:平台開通商家店面"
  - "docs/ssot/dbml/schema.dbml#Table:Merchant"
  - "docs/ssot/haarm/smallBiz.haarm.yaml#roles"
