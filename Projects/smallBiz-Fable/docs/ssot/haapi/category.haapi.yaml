# Generated by rapt-form-haapi
# DSL: haAPI v3.3.0
api: category
schema_version: "3.3"
entity: Category
title: 商品分類 API
description: 多層商品分類維護

exposes:
  standard: [list, read, create, update, delete]
  list:
    filters:
      - {field: storeId, operators: [eq]}
      - {field: parentCategoryId, operators: [eq]}
    sorting: {fields: [sortOrder], default: sortOrder:asc}
    pagination: {style: offset, default_size: 50}

access:
  authentication: {type: bearer, required: true}
  endpoints:
    list:
      required_roles: [public, merchant]
    read:
      required_roles: [public, merchant]
    create:
      required_roles: [merchant]
      required_permissions:
        - id: category_create_store
    update:
      required_roles: [merchant]
      required_permissions:
        - id: category_update_store
    delete:
      required_roles: [merchant]
      required_permissions:
        - id: category_delete_store

source_evidence:
  - "docs/ssot/habdd/product-browsing.ha.feature#Scenario:瀏覽多層分類下的商品"
  - "docs/ssot/dbml/schema.dbml#Table:Category"
  - "docs/ssot/haarm/smallBiz.haarm.yaml#roles"
