# Generated by rapt-form-hapdl
# DSL: haPDL v3.3.0
page: order-list
schema_version: "3.3"
type: list
entity: Order
api: order
title: 訂單列表

auth:
  roles: [consumer, merchant]

view:
  filters:
    - {field: status, label: 訂單狀態, type: eq, ref_code: OrderStatus}
    - {field: createdAt, label: 成立日期, type: range}
  columns:
    - {field: orderId, label: 訂單編號, link: true}
    - {field: status, label: 狀態, format: badge, ref_code: OrderStatus}
    - {field: totalAmount, label: 訂單總額, format: currency}
    - {field: createdAt, label: 成立時間, format: date, sortable: true}
  actions:
    operations: [confirm, ship, cancel]
    placement:
      row: [confirm, ship, cancel]

security:
  permission_refs:
    view:
      - {id: order_list_own}
      - {id: order_list_store}
  datasource_scope: own

source_evidence:
  - "docs/ssot/haapi/order.haapi.yaml"
  - "docs/ssot/habdd/order-fulfillment.ha.feature#Scenario:依狀態篩選訂單"
  - "docs/ssot/haarm/smallBiz.haarm.yaml#permission:order_list_store"
