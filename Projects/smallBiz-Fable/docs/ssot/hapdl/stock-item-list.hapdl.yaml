# Generated by rapt-form-hapdl
# DSL: haPDL v3.3.0
page: stock-item-list
schema_version: "3.3"
type: list
entity: StockItem
api: stock-item
title: 庫存列表

auth:
  roles: [merchant]

view:
  filters:
    - {field: variantId, label: SKU, type: eq}
  columns:
    - {field: variantId, label: SKU, link: true}
    - {field: quantity, label: 實際庫存}
    - {field: reservedQuantity, label: 預留量}
    - {field: restockThreshold, label: 補貨水位}
    - {field: updatedAt, label: 異動時間, format: date, sortable: true}
  actions:
    standard: [update]
    placement:
      row: [update]

security:
  permission_refs:
    view:
      - {id: stock_item_read}
    update:
      - {id: stock_item_update_store}
  datasource_scope: own

source_evidence:
  - "docs/ssot/haapi/stock-item.haapi.yaml"
  - "docs/ssot/habdd/inventory-management.ha.feature#Scenario:設定 SKU 庫存量"
  - "docs/ssot/haarm/smallBiz.haarm.yaml#permission:stock_item_read"
