# Generated by rapt-form-hapdl
# DSL: haPDL v3.3.0
page: coupon-list
schema_version: "3.3"
type: list
entity: Coupon
api: coupon
title: 優惠券列表

auth:
  roles: [merchant, platform-marketer]

view:
  filters:
    - {field: code, label: 券代碼, type: eq}
    - {field: discountType, label: 折扣方式, type: eq, ref_code: CouponDiscountType}
  columns:
    - {field: couponId, label: 優惠券編號, link: true}
    - {field: code, label: 券代碼}
    - {field: name, label: 券名稱}
    - {field: minSpend, label: 最低消費, format: currency}
    - {field: usedCount, label: 已使用}
    - {field: usageCap, label: 張數上限}
    - {field: validTo, label: 有效期迄, format: date, sortable: true}
  actions:
    standard: [create, update]
    placement:
      header: [create]
      row: [update]

security:
  permission_refs:
    view:
      - {id: coupon_read}
    create:
      - {id: coupon_create_store}
    update:
      - {id: coupon_update_store}
  datasource_scope: own

source_evidence:
  - "docs/ssot/haapi/coupon.haapi.yaml"
  - "docs/ssot/habdd/coupon.ha.feature#Scenario:發行優惠券"
  - "docs/ssot/haarm/smallBiz.haarm.yaml#permission:coupon_create_store"
